{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install && npx expo install --fix",
  "buildCommand": "npm run build:web",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
